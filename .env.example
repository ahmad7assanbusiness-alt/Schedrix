# Frontend Environment Variables (for Vercel)
# Set VITE_API_URL in Vercel dashboard to your Railway backend URL
# Example: VITE_API_URL=https://your-app.railway.app

# For local development (client/.env or root .env):
VITE_API_URL=http://localhost:4000

# Backend Environment Variables (for Railway)
# Set these in Railway dashboard under Environment Variables

# DATABASE_URL - PostgreSQL connection string (provided by Railway or Supabase)
# Example: postgresql://user:password@host:5432/database

# JWT_SECRET - Secret key for JWT tokens (generate a strong random string)
# Example: JWT_SECRET=your-super-secret-jwt-key-change-in-production

# PORT - Server port (Railway automatically sets this, but 4000 is the default)
# PORT=4000

# HOST - Server host (defaults to 0.0.0.0 for Railway compatibility)
# HOST=0.0.0.0
